# Aether

A lightweight application template built with Python.

## Development

1. Create `init.py` as the application entry point
3. Build and test the Docker image

## Deployment

Pushing a tag with the `v*` pattern triggers an automated GitHub Actions workflow to build and publish multi-arch images to GHCR
